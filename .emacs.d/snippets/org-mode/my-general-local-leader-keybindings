# -*- mode: snippet -*-
# name: my-general-local-leader-keybindings
# key: general-local-leader-keybindings
# --

(my-local-leader-def
  :keymaps '${mode}-mode-map
  "" '(:ignore t :which-key "my/${mode}")
  "${key}" #'${function}
  )
